<main>
    <div id="title">
        <h1>{{title}}</h1>
    </div>
    <ul>
        {{#if lobbiesLinks}}
            {{#each lobbiesLinks}}
                <li><a href="./{{link}}">Lobby {{link}}</a></li>
            {{/each}}
        {{/if}}
    </ul>
</main>
